// Particles disabled — demo uses a clean background shader with no floating dots.
// This component is kept as a no-op so import references don't break.
export default function Particles() {
  return null;
}
